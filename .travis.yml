language: node_js

node_js:
  - 'node'
  - 'lts/*'

script:
  - npm run test
  - npm run build

before_install:
  - cd Videogames
  - npm install

notifications:
  email:
    on_failure: change

after_success: 'npm run coveralls'

cache:
  directories:
    - node_modules
